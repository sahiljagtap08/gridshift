# Direct browser-to-API base (dev). Leave unset in containers and set API_UPSTREAM instead.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Server-side upstream for the /backend proxy (containers / Azure)
API_UPSTREAM=http://localhost:8000
